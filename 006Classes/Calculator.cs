namespace _006Classes
{
    public class Calculator
    {
        public static int Add(int a, int b) /* We add static here, to use return directly as a variable. If not, we should create object first (as person)*/
        {
            return a + b;
        }
    }

}